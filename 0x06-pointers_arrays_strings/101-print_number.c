#include <stdio.h>
/**
 * *print_number - print numbers with putchar
 * @str: Integer
 * Return: nothing.
 */
int print_number(int n)
{
	int i = 0, res = 0;

	res 
	while (*(str + i))
	{
		for (j = 0; j <= 9; j++)
			if (*(str + i) == leet[j])
				*(str + i) = num[j];
		i++;
	}

	return (str);
}
