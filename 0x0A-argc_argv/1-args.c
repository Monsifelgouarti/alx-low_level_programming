#include <stdio.h>
/**
 * main - it prints the number of arguments 
 * @argc: number of arguments passed to the program
 * Return: 0.
 */
int main(int argc)
{
	printf("%d\n", argc - 1);
	return (0);
}
